apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wanderlust-chart.fullname" . }}-test-connection"
  labels:
    {{- include "wanderlust-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wanderlust-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
